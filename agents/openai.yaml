interface:
  display_name: "Lieflat Charts"
  short_description: "Template-driven charts with context-aware color"
  default_prompt: "Use $lieflat-charts to turn this dataset into a polished HTML chart by reusing the closest Lupi or Basics template, choosing Mono or one coherent color preset from the data and context, and never mixing preset families within one deliverable."

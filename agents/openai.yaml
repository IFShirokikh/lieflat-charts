interface:
  display_name: "Lieflat Charts"
  short_description: "Template-driven charts and bilingual HTML reports"
  default_prompt: "Use $lieflat-charts to turn this data into a polished HTML chart or a complete bilingual-ready report: first decide chart versus report mode, then reuse the closest chart and report templates with one coherent color system."
